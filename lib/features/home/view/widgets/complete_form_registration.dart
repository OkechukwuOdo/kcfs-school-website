import 'package:flutter/material.dart';

class CompleteFormRegistration extends StatelessWidget {
  const CompleteFormRegistration({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}